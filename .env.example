# REQUIRED BY GRADER
BLUE_IMAGE=yimikaade/wonderful:devops-stage-two
GREEN_IMAGE=yimikaade/wonderful:devops-stage-two

# Which pool is active behind Nginx (blue|green)
ACTIVE_POOL=blue

# Release IDs surfaced by the app in X-Release-Id header
RELEASE_ID_BLUE=blue-1.0.0
RELEASE_ID_GREEN=green-1.0.0

# internal app port (host ports are fixed: 8081/8082)
PORT=3000

# --- Alert watcher configuration ---
SLACK_WEBHOOK_URL=
# Percentage (e.g. 2 for 2%) over WINDOW_SIZE that triggers an alert
ERROR_RATE_THRESHOLD=2
# Number of recent requests to consider when computing error rate
WINDOW_SIZE=200
# Minimum seconds between repeated alerts of the same type
ALERT_COOLDOWN_SEC=300
# Set to 1 to silence alerts during planned maintenance
MAINTENANCE_MODE=0
